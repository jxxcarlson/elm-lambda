# File: scripts.yaml

scripts:

  loc: cloc --by-file src

  optimize: elm make --optimize src/Main.elm --output=src/main.js

  make: elm make src/Main.elm --output=src/main.js

  cli: node src/repl.js
